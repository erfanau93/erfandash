# Client-side env (Vite). Copy to `.env.local` (recommended) or `.env` and fill in.
# IMPORTANT: Never put server-only secrets in VITE_* variables.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Public tokens (ok in browser, but should not be hardcoded in source)
VITE_MAPBOX_TOKEN=
VITE_GOOGLE_REVIEW_URL=


